spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/itamartafarello/Documents/repository/ai-sandbox/config-server-repository
          clone-on-start: true
    fail-fast: true